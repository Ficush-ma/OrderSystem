server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/OrderSystem?serverTimezone=UTC
    username: root
    password: Mamaguai123.@
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    timeout: 2000
    database: 1
rocketmq:
  name-server: localhost:9876
  producer:
    group: order-producer
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.orderSystem.model
  configuration:
    map-underscore-to-camel-case: true